import math, random
a = [random.randint(1,11) for i in range(10)]
a
[9, 3, 2, 8, 3, 5, 8, 11, 2, 3]
#print (a)
from math import ceil, floor
import csv
import statistics as st #import statistics module under st



try:
    smpl_mode = st.mode(sample_sorted)
except st.StatisticsError:
    smpl_mode = "multimode distorbution"
smpl_mean = st.mean(sample_sorted) #mode
smpl_median = st.median(sample_sorted) #median



      




